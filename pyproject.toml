[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fabric-semantic-model-governance"
version = "0.1.0"
description = "Fabric solution accelerator for duplicate Power BI semantic model governance"
requires-python = ">=3.10"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
  { name = "fabric_duplicate_semanticmodel_app contributors" }
]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

